# Changelog

## 0.2.0

- Replace permissive fallback with explicit tool/operation policies.
- Validate configuration, action types, schemas, domains and destinations.
- Add trusted bindings, immutable argument snapshots and guarded dispatch.
- Add HMAC approvals with TTL, context binding and persistent atomic replay protection.
- Add minimal SQLite audit events and fail-closed pre-dispatch audit handling.
- Add optional stdio MCP bridge and a real subprocess enforcement test.
- Add policy simulation, comparison, CI regression checks and domain policy examples.
- Correct contract-example behavior and document migration and trust boundaries.
- Add local demo, lint/format checks, package builds and a Python CI matrix.

## 0.1.0

- Initial policy evaluator, JSON CLI, YAML configuration and four tests.
